# LifeOS Portfolio Dashboard — Environment Variables
# Copy this file to .env.local and fill in real values before running locally with Supabase.
# Never commit .env or .env.local to the repository.

# --- Phase 12: Supabase (add when database persistence is ready) ---
# VITE_SUPABASE_URL=https://your-project-ref.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key-here
